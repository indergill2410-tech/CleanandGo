name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci
        env:
          # Retry transient registry network errors (e.g. ECONNRESET) instead
          # of failing the whole job on the first blip.
          npm_config_fetch_retries: "5"
          npm_config_fetch_retry_factor: "3"
          npm_config_fetch_retry_mintimeout: "10000"
          npm_config_fetch_retry_maxtimeout: "60000"

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build
        env:
          # Public placeholders so the build (which doesn't talk to any service)
          # can run without real secrets. Server SDK clients are lazily created.
          NEXT_PUBLIC_SUPABASE_URL: https://placeholder.supabase.co
          NEXT_PUBLIC_SUPABASE_ANON_KEY: placeholder-anon-key
          NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: pk_test_placeholder
          NEXT_PUBLIC_APP_URL: https://example.com
